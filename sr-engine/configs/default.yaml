# sr-engine default configuration
device: cuda
seed: 42
dtype: bf16
scale: 4
patch_size: 128
batch_size: 16
num_workers: 4
learning_rate: 2e-4
weight_decay: 0.0
betas: [0.9, 0.99]
max_steps: 300000
log_every: 100
save_every: 5000
eval_every: 5000
tile_size: 512
tile_overlap: 64
